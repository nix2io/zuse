import { join } from "path";

export const ROOT_DIR = join("../../");
export const SPECIFICATION_FILE = join(ROOT_DIR, "spec.yaml");
export const LANG_DIR = join(ROOT_DIR, "lang/");

export const GENERATED_SPEC_FILE_NAME = "specification.json";
